# examples

Each example is a small agent that runs **unmodified except for its `base_url`**,
pointed at a local `agentvcr serve`.

Planned (PLAN.md):

- `plain-loop/` — a scripted tool loop on the OpenAI SDK (phase 1, the record/replay contract)
- `openai-agents-sdk/`, `langgraph/`, `crewai/` — one-line `base_url` change each (phase 6)
